services:
  - type: web
    name: sis315-dynamics365
    env: node
    plan: free
    buildCommand: "cd backend && npm install && cd ../frontend && npm install --include=dev && npx vite build"
    startCommand: "cd backend && npm start"
    envVars:
      - key: NODE_ENV
        value: production
